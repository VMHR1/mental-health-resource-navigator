# API

Serverless endpoints live under `functions/api/`.

## Purpose
- Handle submissions, admin actions, or data endpoints that should not run purely in the browser.

## How it relates to the frontend
- Frontend calls these endpoints for actions like submission or admin workflows (if implemented).
